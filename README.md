# Rip
This program takes as input a string of parentheses and is removing the minimum amount of '(' and ')' to make the expression balanced (all open parenthesis are closed). The program will print ALL possibel minimal removals
